type Food @model @auth(rules: [{allow: public, operations: [read]}, {allow: owner}]) {
  id: ID!
  name: String!
  pounds: String!
  daysOld: String!
  pickUp: String!
  restaurantID: ID! @index(name: "byRestaurant")
}

type Restaurant @model @auth(rules: [{allow: owner}, {allow: public, operations: [read]}]) {
  id: ID!
  name: String!
  address: String!
  phone: String!
  email: String!
  Foods: [Food] @hasMany(indexName: "byRestaurant", fields: ["id"])
}
